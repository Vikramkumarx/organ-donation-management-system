<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donation Form</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: #FFF5EE;
        }

        .header {
            background: #fcc358;
            padding: 25px;
            color: white;
            text-align: center;
        }

        .wrapper {
            max-width: 600px;
            margin: 50px auto;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>
    <div class="header">
        <h3>Complete Donation Details</h3>
    </div>

    <div class="wrapper">
        <h5>Donating for: <%= request.ORGAN_TYPE %> (<%= request.BLOOD_GROUPn %>)</h5>
        <hr>
        <form action="/donate/<%= request.REQUEST_ID %>" method="post">
            <div class="form-group">
                <label>Weight (kg)</label>
                <input type="number" step="0.1" name="WEIGHT" class="form-group form-control" required>
            </div>
            <div class="form-group">
                <label>BMI</label>
                <input type="number" step="0.1" name="BMI" class="form-control" required>
            </div>
            <div class="form-group">
                <label>Any Past Operation Type?</label>
                <input type="text" name="OPERATION_TYPE" class="form-control" placeholder="None if none">
            </div>
            <div class="form-group">
                <label>Operation Description</label>
                <textarea name="OPERATION_DESC" class="form-control"></textarea>
            </div>
            <div class="form-group">
                <label>Any Disease?</label>
                <input type="text" name="DISEASE_TYPE" class="form-control" placeholder="None if none">
            </div>
            <div class="form-group">
                <label>Disease Description</label>
                <textarea name="DISEASE_DESC" class="form-control"></textarea>
            </div>
            <div class="form-group">
                <label>Quantity to Donate</label>
                <input type="number" name="QUANTITYd" class="form-control" value="1" min="1" required>
            </div>
            <button type="submit" class="btn btn-success btn-block">Confirm Donation</button>
            <a href="/available_to_donate" class="btn btn-secondary btn-block">Cancel</a>
        </form>
    </div>
</body>

</html>