<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donation Record</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: #FFF5EE;
        }

        .header {
            background: #fcc358;
            padding: 30px;
            color: white;
            text-align: center;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-dark bg-dark">
        <a class="navbar-brand" href="/logged_in">Home</a>
        <a class="btn btn-warning" href="/logout">Logout</a>
    </nav>

    <div class="header">
        <h3>Your Donation Records</h3>
    </div>

    <div class="container mt-5">
        <table class="table table-bordered table-striped">
            <thead class="thead-dark">
                <tr>
                    <th>Blood Group</th>
                    <th>Quantity</th>
                    <th>Date Donated</th>
                </tr>
            </thead>
            <tbody>
                <% if (records.length===0) { %>
                    <tr>
                        <td colspan="3" class="text-center">No donation records found.</td>
                    </tr>
                    <% } %>
                        <% records.forEach(rec=> { %>
                            <tr>
                                <td>
                                    <%= rec.BLOOD_GROUPd %>
                                </td>
                                <td>
                                    <%= rec.QUANTITYd %>
                                </td>
                                <td>
                                    <%= rec.last_donated %>
                                </td>
                            </tr>
                            <% }) %>
            </tbody>
        </table>
        <div class="text-center mt-4">
            <a href="/logged_in" class="btn btn-secondary">Back to Home</a>
        </div>
    </div>
</body>

</html>